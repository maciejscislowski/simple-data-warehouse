build:
  docker:
    web: Dockerfile
setup:
  config:
    PROFILE: heroku
#run:
#  web: java $JAVA_OPTS -Dserver.port=$PORT -jar target/eadmissions-loader-0.0.1-SNAPSHOT.jar
#  web: java $JAVA_OPTS -Dserver.port=$PORT -jar /app.jar
